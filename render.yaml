services:
  - type: web
    name: ea-trade-web
    env: docker
    plan: free
    autoDeploy: true
    healthCheckPath: /
    envVars:
      - key: EXPO_NO_TELEMETRY
        value: "1"
      - key: NODE_ENV
        value: "production"

